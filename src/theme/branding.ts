/**
 * Branding placeholders.
 *
 * Binary assets (png/jpg/webp/etc.) are intentionally not included.
 * Add manual files later under assets/branding and wire them in here.
 */
export const branding = {
  appName: 'Padel Reservas',
  logoPlaceholder: 'PR'
} as const;
